#include "shell.h"

/**
* shell_show_prompt - prints $ to let user know the program is
* ready to take their input
* prints the prompt if the shell is in interactive mode
* Return: no return
*/

void shell_show_prompt(void)
{
	shell_printf("grp_shell$ ");
}

